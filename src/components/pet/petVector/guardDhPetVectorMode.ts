/**
 * Modo avatar vector (cuerpo SVG aproximado + cara procedural).
 *
 * @deprecated 2026-Q2: rama **inactiva** en producción (`USE_GUARD_DH_VECTOR_PET` en `petRuntimeConfig`).
 * Antes de reactivar: abrí un issue de producto/arte (enlace al ticket aquí cuando exista) y validá
 * paridad visual con `public/brand/pet.svg`. Hasta entonces mantener `false` para evitar bitrot silencioso.
 */
export { USE_GUARD_DH_VECTOR_PET } from '@/lib/pet/petRuntimeConfig'
