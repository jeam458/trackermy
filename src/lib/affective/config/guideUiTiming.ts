/** Umbrales de tiempo UX / coach (ms). Datos aparte del código de efectos. */

/** Coach LLM por clicks repetidos en lienzo del mapa (Leaflet). */
export const GUIDE_MAP_CANVAS_COACH_THROTTLE_MS = 8000

/** Debounce scroll principal → disparador dinámico `ui.scroll_main`. */
export const GUIDE_MAIN_SCROLL_DEBOUNCE_MS = 950

/** Bucketización aproximada de scrollTop en px (agrupa eventos similares). */
export const GUIDE_MAIN_SCROLL_BUCKET_PX = 120
